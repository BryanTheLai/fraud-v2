---
id: RR-20260505-028
task: TC-20260505-028
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Stream Lag CLI

## Assignment

Add local Redpanda consumer-group lag inspection.

## What Changed

- Added `RedpandaLagProbe`.
- Added `fraud-v2 stream-lag`.
- Added partition-level lag report fields.
- Added unit coverage for lag math.
- Expanded full-profile smoke to assert zero consumer-group lag after the
  valid stream consume.
- Updated docs, PR draft, and version metadata to `0.28.0`.

## Proof Captured

Fresh local verification passed:

```powershell
uv run ruff format .
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
uv run pytest --collect-only -q
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Observed proof:

- Ruff format/check: passed.
- Ruff lint: passed.
- Mypy: passed across 75 source files.
- Pytest: passed with 74 collected tests.
- Docker Compose full-profile config: passed.
- Docker image build: passed for `fraud-v2:local` and installed
  `fraud-v2==0.28.0`.
- Full-profile smoke: passed with API/Postgres/Redis/Neo4j/Redpanda/
  Grafana/Prometheus checks.
- Valid stream proof: `ingested: 1`, `failed: 0`, `dead_lettered: 0`.
- Stream lag proof after valid consume: committed offset `1`, high watermark
  `1`, partition lag `0`, and total lag `0`.
- Invalid stream proof still passes: `dead_lettered: 1`,
  `dead_letter_published: 1`, `failed: 1`, and Redpanda DLQ topic proof
  printed `redpanda_dlq=1`.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
