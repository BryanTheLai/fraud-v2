---
id: RR-20260505-025
task: TC-20260505-025
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Stream Dead Letters

## Assignment

Persist stream failures as local dead letters and expose them for operator
inspection.

## What Changed

- Added `StreamDeadLetter` and dead-letter reason contracts.
- Added SQLite/Postgres persistence for stream dead letters.
- Updated the stream consumer to write dead letters for message errors, empty
  payloads, invalid events, and idempotency conflicts before committing the
  offset.
- Added admin API and CLI listing paths.
- Added retention report/prune coverage for stream dead letters.
- Expanded tests and full-profile smoke.
- Updated docs, PR draft, and version metadata to `0.25.0`.

## Proof Captured

Fresh local verification passed:

```powershell
uv run ruff format .
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Observed proof:

- Ruff format/check: passed.
- Ruff lint: passed.
- Mypy: passed across 72 source files.
- Pytest: passed with 64 tests.
- Docker Compose full-profile config: passed.
- Docker image build: passed for `fraud-v2:local` and installed
  `fraud-v2==0.25.0`.
- Full-profile smoke: passed with API/Postgres/Redis/Neo4j/Redpanda/
  Grafana/Prometheus checks.
- Stream proof: valid Redpanda consume reported `dead_lettered: 0`, `ingested:
  1`, and `failed: 0`; Postgres proof printed `redpanda_consume=1`; admin dead
  letter endpoint returned no dead letters for the valid smoke event.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
