---
id: RR-20260505-005
task: TC-20260505-005
created_at: 2026-05-05
status: needs_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Harden Local API Auth

## Assignment

Require credentials on protected local API routes by default.

## What Changed

- Missing or wrong bearer token now returns `401` for protected `/v1/*` routes.
- Empty `FRAUD_API_TOKEN` remains an explicit local opt-out.
- Integration tests cover authorized and missing-token paths.
- Bumped package/API version to `0.5.0`.

## Proof Captured

```powershell
uv sync --extra dev --extra infra --extra llm
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
```

Exit code: 0. Final Docker build installed `fraud-v2==0.5.0` and returned live
health from the container smoke.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login`.
