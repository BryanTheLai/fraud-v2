---
id: RR-20260505-022
task: TC-20260505-022
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add JWKS Auth

## Assignment

Add offline-testable JWKS/OIDC-shaped JWT verification while preserving the
local HS256 token path.

## What Changed

- Added JWT algorithm configuration.
- Added local JWKS file verification.
- Added direct JWKS URL support.
- Added OIDC discovery URL support for `jwks_uri`.
- Refused HS algorithms when JWKS verification is configured.
- Kept local HS256 JWT auth working.
- Added `PyJWT[crypto]` runtime support.
- Added RS256 JWKS unit coverage.
- Updated docs and PR draft.
- Bumped package/API version to `0.22.0`.

## Proof Captured

Fresh local verification passed:

```powershell
uv run ruff format .
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Observed proof:

- Ruff format/check: passed.
- Ruff lint: passed.
- Mypy: passed across 68 source files.
- Pytest: passed with 56 tests.
- JWT auth unit tests: passed, including RS256 token validation from local JWKS.
- Docker Compose full-profile config: passed.
- Docker image build: passed for `fraud-v2:local` and installed
  `fraud-v2==0.22.0` with `cryptography`.
- Full-profile smoke: passed with existing API/Postgres/Redis/Neo4j/Redpanda/
  Grafana/Prometheus checks.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
