---
id: RR-20260505-024
task: TC-20260505-024
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Versioned Threshold Policy

## Assignment

Add a local-safe versioned threshold policy boundary for the decision engine.

## What Changed

- Added `ThresholdPolicy` with validated green/yellow/red bands, degraded floor,
  high-amount threshold, severity, safe reason, and version.
- Updated the decision engine to use the active policy instead of hardcoded
  thresholds.
- Added CLI/API policy loading through `--policy-path` and
  `FRAUD_POLICY_PATH`.
- Added `fraud-v2 policy-show`.
- Added unit coverage for policy loading, validation, and custom decision
  outcomes.
- Updated docs, PR draft, and version metadata to `0.24.0`.

## Proof Captured

Fresh local verification passed:

```powershell
uv run ruff format .
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Observed proof:

- Ruff format/check: passed.
- Ruff lint: passed.
- Mypy: passed across 71 source files.
- Pytest: passed with 63 tests.
- Docker Compose full-profile config: passed.
- Docker image build: passed for `fraud-v2:local` and installed
  `fraud-v2==0.24.0`.
- Full-profile smoke: passed with API/Postgres/Redis/Neo4j/Redpanda/
  Grafana/Prometheus checks.
- Stream proof remained green: `stream-consume` reported `ingested: 1` and
  `failed: 0`, then Postgres proof printed `redpanda_consume=1`.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
