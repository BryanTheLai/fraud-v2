---
id: RR-20260505-017
task: TC-20260505-017
created_at: 2026-05-05
status: needs_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Review Feedback Events

## Assignment

Close the local active-learning loop by turning analyst review outcomes into
canonical events and training labels.

## What Changed

- Updated `ReviewService.decide()` to append a `MANUAL_REVIEW_DECIDED` event.
- Added `LABEL_CREATED` feedback events for `CONFIRMED_FRAUD` and
  `CONFIRMED_LEGITIMATE` outcomes.
- Kept non-final review outcomes out of training labels.
- Added tests for confirmed and non-final review outcomes.
- Updated docs and PR draft.
- Bumped package/API version to `0.17.0`.

## Proof Captured

```powershell
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
```

Exit code: 0.

Key output:

```text
83 files already formatted
All checks passed!
Success: no issues found in 66 source files
................................................                         [100%]
```

```powershell
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
```

Exit code: 0. Docker build installed `fraud-v2==0.17.0`.

```powershell
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Exit code: 0.

Key output:

```text
API ready: http://127.0.0.1:18000/health/live
Prometheus ready: http://127.0.0.1:19090/-/ready
Grafana ready: http://127.0.0.1:13000/api/health
Neo4j ready: http://127.0.0.1:17474/
Postgres ready
Redis ready
Redpanda ready
Neo4j Bolt ready
postgres_events=1
redis_feature_cache=user_00000
neo4j_edges=50
redpanda_publish=1
Prometheus query ready: up{job="fraud-v2-api", instance="api:8000"}
```

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
