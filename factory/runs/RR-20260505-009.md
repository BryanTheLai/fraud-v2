---
id: RR-20260505-009
task: TC-20260505-009
created_at: 2026-05-05
status: needs_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Polish Analyst Dashboard

## Assignment

Make the local analyst dashboard useful for reviewing recent decisions and open
review cases.

## What Changed

- Added recent decision table to `/dashboard`.
- Added open review queue table to `/dashboard`.
- Added dashboard integration assertions.
- Bumped package/API version to `0.9.0`.

## Proof Captured

```powershell
uv sync --extra dev --extra infra --extra llm
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
```

Exit code: 0.

Key output:

```text
73 files already formatted
All checks passed!
Success: no issues found in 61 source files
...............................                                          [100%]
```

```powershell
python TestClient dashboard smoke
```

Exit code: 0.

Output:

```text
200
True
True
True
```

```powershell
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
docker run -d -p 8011:8000 fraud-v2:local
Invoke-WebRequest -Uri http://127.0.0.1:8011/dashboard
```

Exit code: 0. Docker build installed `fraud-v2==0.9.0`.

Output:

```text
dashboard_status=200
has_recent_decisions=True
has_open_review_queue=True
```

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login`.
