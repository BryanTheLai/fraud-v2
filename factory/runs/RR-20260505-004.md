---
id: RR-20260505-004
task: TC-20260505-004
created_at: 2026-05-05
status: needs_review
repo: fraud-v2
area: implementation
approval: yellow
---

# Run Record: Compliance And Cost Evaluation

## Assignment

Add local-safe compliance draft exports and fraud-specific threshold evaluation.

## What Changed

- Added `ComplianceDraft` contract and JSON writer.
- Added `fraud-v2 compliance-draft` for stored decisions.
- Added cost assumptions and cost-weighted threshold candidate reporting.
- Added `cost_weighted_threshold`, `recall_under_1pct_fpr_threshold`, and
  candidate thresholds to baseline training reports.
- Bumped package/API version to `0.4.0`.
- Added tests for compliance drafts and threshold reporting.

## Proof Captured

```powershell
uv sync --extra dev --extra infra --extra llm
```

Exit code: 0.

```powershell
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
```

Exit code: 0.

Key output before final version bump:

```text
69 files left unchanged
All checks passed!
Success: no issues found in 59 source files
..........................                                               [100%]
```

```powershell
uv run fraud-v2 train --events-path data/synthetic/tiny/events.jsonl --output-dir data/models/baseline
```

Exit code: 0.

Key output:

```json
{
  "cost_weighted_threshold": {
    "profit": 1999.964,
    "recall": 1.0,
    "threshold": 0.39999999999999997
  },
  "recall_under_1pct_fpr_threshold": {
    "fpr": 0.0,
    "recall": 1.0,
    "threshold": 0.39999999999999997
  }
}
```

```powershell
uv run fraud-v2 compliance-draft <decision-id> --db-path data/local/compliance-smoke.sqlite --output-path data/local/compliance-draft-smoke.json
```

Exit code: 0.

Key output:

```json
{
  "human_review_required": true,
  "risk_tier": "RED",
  "simulated_action": "BLOCK"
}
```

```powershell
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
docker run -d -p 8010:8000 fraud-v2:local
Invoke-RestMethod -Uri http://127.0.0.1:8010/health/live
```

Exit code: 0. Final image installed `fraud-v2==0.4.0` and returned live health.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login`.

## Remaining Gap To Full Blog

- Docker-backed integration tests for full profile services.
- Analyst UI beyond the current dashboard.
- Model registry and shadow deployment controls.
- Production RBAC, secrets, retention, legal review, and real vendor contracts.
