---
id: RR-20260505-029
task: TC-20260505-029
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Stream Supervisor CLI

## Assignment

Add a local-safe supervised stream worker mode for repeated bounded Redpanda
consume batches.

## What Changed

- Added `StreamSupervisor`.
- Added `fraud-v2 stream-supervise`.
- Added aggregate supervisor reporting for batch health and stream outcomes.
- Added unit coverage for successful batches, transient recovery, and failure
  threshold behavior.
- Expanded full-profile smoke to publish a supervised stream event and prove
  the supervisor ingests it into Postgres.
- Updated docs, PR draft, and version metadata to `0.29.0`.

## Proof Captured

Fresh local verification passed:

```powershell
uv run ruff format .
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
uv run pytest --collect-only -q
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Observed proof:

- Ruff format/check: passed.
- Ruff lint: passed.
- Mypy: passed across 76 source files.
- Pytest: passed with 77 collected tests.
- Docker Compose full-profile config: passed.
- Docker image build: passed for `fraud-v2:local` and installed
  `fraud-v2==0.29.0`.
- Full-profile smoke: passed with API/Postgres/Redis/Neo4j/Redpanda/
  Grafana/Prometheus checks.
- Stream supervisor proof: `status: completed`, `completed_batches: 1`,
  `failed_batches: 0`, `ingested: 1`, and `message_failures: 0`.
- Stream lag proof still passes after valid consume: total lag `0`.
- Invalid stream proof still passes: `dead_lettered: 1`,
  `dead_letter_published: 1`, `failed: 1`, and Redpanda DLQ topic proof
  printed `redpanda_dlq=1`.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
