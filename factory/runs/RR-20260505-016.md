---
id: RR-20260505-016
task: TC-20260505-016
created_at: 2026-05-05
status: needs_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Prove Remaining Infra Adapters

## Assignment

Expand the full Docker profile smoke from container-readiness checks to adapter
proof for Redis, Neo4j, and Redpanda.

## What Changed

- Expanded `scripts/full-smoke.ps1` to wait for:
  - Redis with `redis-cli ping`
  - Redpanda with `rpk cluster info`
  - Neo4j Bolt with `cypher-shell`
- Added API-container smoke snippets for:
  - `RedisFeatureCache.put()` and `RedisFeatureCache.get()`
  - `Neo4jGraphProjector.project()`
  - `RedpandaEventPublisher.publish()`
- Fixed Redpanda Compose advertised listeners so API-container clients use
  `redpanda:9092` and laptop clients use `localhost:19092`.
- Added tests proving the full-smoke coverage.
- Updated docs and PR draft.
- Bumped package/API version to `0.16.0`.

## Proof Captured

```powershell
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
```

Exit code: 0.

Key output:

```text
82 files already formatted
All checks passed!
Success: no issues found in 66 source files
..............................................                           [100%]
```

```powershell
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
```

Exit code: 0. Docker build used the cached `fraud-v2==0.16.0` image layer.

```powershell
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Exit code: 0.

Key output:

```text
API ready: http://127.0.0.1:18000/health/live
Prometheus ready: http://127.0.0.1:19090/-/ready
Grafana ready: http://127.0.0.1:13000/api/health
Neo4j ready: http://127.0.0.1:17474/
Postgres ready
Redis ready
Redpanda ready
Neo4j Bolt ready
postgres_events=1
redis_feature_cache=user_00000
neo4j_edges=50
redpanda_publish=1
Prometheus query ready: up{job="fraud-v2-api", instance="api:8000"}
```

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
