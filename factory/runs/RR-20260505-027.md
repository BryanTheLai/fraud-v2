---
id: RR-20260505-027
task: TC-20260505-027
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Redpanda DLQ Publisher

## Assignment

Add optional Redpanda DLQ topic publishing for stream dead letters.

## What Changed

- Added `RedpandaDeadLetterPublisher`.
- Added `--publish-dead-letters` and `--dead-letter-topic` to
  `fraud-v2 stream-consume`.
- Updated stream worker reporting with `dead_letter_published` and
  `dead_letter_publish_failed`.
- Kept DB-only dead letters as the default local path.
- Added unit tests for successful and failed DLQ publishing.
- Expanded full-profile smoke to publish an invalid stream record, consume it,
  publish a dead letter to Redpanda, and verify the DLQ topic payload.
- Updated docs, PR draft, and version metadata to `0.27.0`.

## Proof Captured

Fresh local verification passed:

```powershell
uv run ruff format .
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Observed proof:

- Ruff format/check: passed.
- Ruff lint: passed.
- Mypy: passed across 74 source files.
- Pytest: passed with 70 tests.
- Docker Compose full-profile config: passed.
- Docker image build: passed for `fraud-v2:local` and installed
  `fraud-v2==0.27.0`.
- Full-profile smoke: passed with API/Postgres/Redis/Neo4j/Redpanda/
  Grafana/Prometheus checks.
- Valid stream proof: `ingested: 1`, `failed: 0`, `dead_lettered: 0`.
- Invalid stream proof: `dead_lettered: 1`, `dead_letter_published: 1`,
  `failed: 1`, and Redpanda DLQ topic proof printed `redpanda_dlq=1`.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
