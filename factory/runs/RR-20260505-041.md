---
id: RR-20260505-041
task: TC-20260505-041
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Capacity Profile

## Assignment

Add a named local capacity profile receipt over the existing synthetic load
benchmark.

## What Changed

- Added `CapacityProfile` presets for `smoke`, `laptop`, and `stress`.
- Added `run_capacity_profile`.
- Added `fraud-v2 capacity-profile`.
- Added JSON and HTML capacity report artifacts.
- Added target checks for load events/sec and score decisions/sec.
- Added `--fail-on-target-miss` for release rehearsals.
- Added GitHub Actions capacity-profile smoke artifact upload.
- Added unit coverage for passing, warning, and profile override behavior.
- Updated docs, PR draft, and version metadata to `0.41.0`.

## Proof Captured

- `uv run ruff format .`: pass.
- `uv run ruff format --check .`: pass, 121 files already formatted.
- `uv run ruff check .`: pass.
- `uv run fraud-v2 secrets-scan --root .`: pass, 242 files scanned, zero findings.
- `uv run mypy src`: pass, 87 source files checked.
- `uv run pytest -q`: pass.
- `uv run pytest --collect-only -q`: pass, 107 collected tests.
- `uv run fraud-v2 capacity-profile --profile smoke --users 50 --score-users 5 --min-load-events-per-second 0.1 --min-score-decisions-per-second 0.1 --output-dir data\local\ci-capacity --overwrite --fail-on-target-miss`:
  pass. It generated 316 events, inserted 316 events, loaded at 131.951
  events/sec, scored at 61.006 decisions/sec, and wrote JSON/HTML artifacts.
- `docker compose -f infra\docker-compose.yml --profile full config --quiet`: pass.
- `docker build -t fraud-v2:local .`: pass, installed `fraud-v2==0.41.0`.
- `powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1 -TimeoutSeconds 240`:
  pass. Smoke covered API, Postgres, Redis, Neo4j, Redpanda, Grafana,
  Prometheus, audit archive proof with 196 valid hash-chain entries, zero stream
  lag, healthy stream report with `health_score: 100`, DLQ proof, and local
  trace report proof with 17 spans.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
