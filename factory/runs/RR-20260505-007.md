---
id: RR-20260505-007
task: TC-20260505-007
created_at: 2026-05-05
status: needs_review
repo: fraud-v2
area: implementation
approval: yellow
---

# Run Record: Shadow Scoring

## Assignment

Score registered models in shadow without changing production decisions.

## What Changed

- Added `ShadowScore` contract.
- Added registered-model shadow scoring over local event streams.
- Added `fraud-v2 shadow-score`.
- Shadow report writes probability, threshold, would-flag, model status, target
  entity, and feature values.
- Bumped package/API version to `0.7.0`.
- Added unit test coverage for shadow scoring.

## Proof Captured

```powershell
uv sync --extra dev --extra infra --extra llm
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
```

Exit code: 0.

Key output:

```text
73 files left unchanged
All checks passed!
Success: no issues found in 61 source files
...............................                                          [100%]
```

```powershell
uv run fraud-v2 train --events-path data/synthetic/tiny/events.jsonl --output-dir data/models/baseline
uv run fraud-v2 model-register --status shadow --notes "shadow scoring smoke"
uv run fraud-v2 model-promote baseline-20260505-001
uv run fraud-v2 shadow-score --events-path data/synthetic/tiny/events.jsonl --registry-path data/models/registry.json --output-path data/models/shadow-scores.json --status active
```

Exit code: 0.

Key output:

```json
{
  "model_status": "active",
  "model_version": "baseline-20260505-001",
  "rows": 120,
  "would_flag": 8
}
```

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login`.

## Docker Proof

```powershell
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
docker run -d -p 8010:8000 fraud-v2:local
Invoke-RestMethod -Uri http://127.0.0.1:8010/health/live
```

Exit code: 0. Final Docker build installed `fraud-v2==0.7.0` and returned live
health from the container smoke.

## Remaining Gap To Full Blog

- Docker-backed integration tests for full profile services.
- Analyst UI beyond the current dashboard.
- Production RBAC, secrets, retention, legal review, and real vendor contracts.
