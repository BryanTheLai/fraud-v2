---
id: RR-20260505-015
task: TC-20260505-015
created_at: 2026-05-05
status: needs_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Prove Postgres Adapter

## Assignment

Prove the Postgres event-store adapter works in the Docker full profile without
changing the default API store.

## What Changed

- Updated `Dockerfile` to install `--extra infra` in the API image.
- Expanded `scripts/full-smoke.ps1` to:
  - wait for Postgres with `pg_isready`
  - run `PostgresEventStore.init_schema()`
  - insert a synthetic event
  - list events back from Postgres
- Added tests proving the Dockerfile and full-smoke coverage.
- Updated docs and PR draft.
- Bumped package/API version to `0.15.0`.

## Proof Captured

```powershell
uv run ruff format --check .
uv run ruff check .
uv run mypy src tests
uv run pytest -q
```

Exit code: 0.

Key output:

```text
82 files already formatted
All checks passed!
Success: no issues found in 82 source files
.............................................                            [100%]
```

```powershell
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
```

Exit code: 0. Docker build installed `fraud-v2==0.15.0` with
`psycopg`, `redis`, `neo4j`, and `confluent-kafka` from the `infra` extra.

```powershell
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Exit code: 0.

Key output:

```text
API ready: http://127.0.0.1:18000/health/live
Prometheus ready: http://127.0.0.1:19090/-/ready
Grafana ready: http://127.0.0.1:13000/api/health
Neo4j ready: http://127.0.0.1:17474/
Postgres ready
postgres_events=1
Prometheus query ready: up{job="fraud-v2-api", instance="api:8000"}
```

Note: `pwsh` is not installed on this laptop. The smoke script was verified
with built-in Windows PowerShell.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
