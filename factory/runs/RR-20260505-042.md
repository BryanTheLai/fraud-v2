---
id: RR-20260505-042
task: TC-20260505-042
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Postgres Backup Rehearsal

## Assignment

Add a local full-profile Postgres backup and scratch-restore rehearsal.

## What Changed

- Added `scripts/postgres-backup-rehearsal.ps1`.
- Added `pg_dump -Fc` backup creation through Docker Compose.
- Added local dump copy and SHA-256 manifest writing.
- Added scratch database restore through `pg_restore`.
- Added source/restored event-count verification.
- Added default scratch database cleanup with `-KeepRestoreDatabase` for manual
  inspection.
- Wired the rehearsal into `scripts/full-smoke.ps1`.
- Added unit coverage for script contents and full-smoke wiring.
- Updated docs, PR draft, and version metadata to `0.42.0`.

## Proof Captured

- `uv run ruff format .`: pass.
- `uv run ruff format --check .`: pass, 122 files already formatted.
- `uv run ruff check .`: pass.
- `uv run fraud-v2 secrets-scan --root .`: pass, 246 files scanned, zero findings.
- `uv run mypy src`: pass, 87 source files checked.
- `uv run pytest -q`: pass.
- `uv run pytest --collect-only -q`: pass, 109 collected tests.
- PowerShell parser check for `scripts\postgres-backup-rehearsal.ps1` and
  `scripts\full-smoke.ps1`: pass.
- `uv run fraud-v2 capacity-profile --profile smoke --users 50 --score-users 5 --min-load-events-per-second 0.1 --min-score-decisions-per-second 0.1 --output-dir data\local\ci-capacity --overwrite --fail-on-target-miss`:
  pass. It generated 316 events, loaded at 119.46 events/sec, scored at 64.243
  decisions/sec, and wrote JSON/HTML artifacts.
- `docker compose -f infra\docker-compose.yml --profile full config --quiet`: pass.
- `docker build -t fraud-v2:local .`: pass, installed `fraud-v2==0.42.0`.
- `powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1 -TimeoutSeconds 240`:
  pass. Smoke covered API, Postgres, Redis, Neo4j, Redpanda, Grafana,
  Prometheus, audit archive proof with 196 valid hash-chain entries, Postgres
  backup rehearsal with an 80,119-byte `pg_dump_custom` artifact, source/restored
  event counts of 194/194, `verified: true`, zero stream lag, healthy stream
  report with `health_score: 100`, DLQ proof, and local trace report proof with
  18 spans.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
