---
id: RR-20260505-038
task: TC-20260505-038
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Secrets Scan

## Assignment

Add a local and CI secrets scan for real-looking committed credentials.

## What Changed

- Added `scan_secrets` and masked finding reports.
- Added `fraud-v2 secrets-scan`.
- Added tests for real-looking token detection and documented placeholder
  allowance.
- Added the secrets scan to GitHub Actions.
- Updated docs, PR draft, and version metadata to `0.38.0`.

## Proof Captured

- `uv run ruff format .`: pass
- `uv run ruff format --check .`: pass, 115 files already formatted
- `uv run ruff check .`: pass
- `uv run fraud-v2 secrets-scan --root .`: pass, 230 files scanned, zero findings
- `uv run mypy src`: pass, 84 source files
- `uv run pytest -q`: pass
- `uv run pytest --collect-only -q`: pass, 101 collected tests
- `docker compose -f infra\docker-compose.yml --profile full config --quiet`: pass
- `docker build -t fraud-v2:local .`: pass, installed `fraud-v2==0.38.0`
- `powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1 -TimeoutSeconds 240`: pass, including API, Postgres, Redis, Neo4j, Redpanda, stream lag `0`, stream supervisor `completed_batches: 1`, stream health `status: healthy`, trace report `total_spans: 17`, DLQ publish proof, Grafana, Prometheus, retention prune, and audit verification.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
