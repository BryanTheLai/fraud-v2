---
id: RR-20260505-008
task: TC-20260505-008
created_at: 2026-05-05
status: needs_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Full Profile Smoke

## Assignment

Add a repeatable local verification script for the Docker-backed full profile.

## What Changed

- Added `scripts/full-smoke.ps1`.
- Script validates compose config, starts the full profile with build, checks
  API/Prometheus/Neo4j HTTP readiness, prints compose status, and stops services
  unless `-KeepRunning` is passed.
- Bumped package/API version to `0.8.0`.

## Proof Captured

```powershell
uv sync --extra dev --extra infra --extra llm
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
```

Exit code: 0.

Key output:

```text
73 files left unchanged
All checks passed!
Success: no issues found in 61 source files
...............................                                          [100%]
```

```powershell
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
```

Exit code: 0. Docker build installed `fraud-v2==0.8.0`.

```powershell
.\scripts\full-smoke.ps1 -TimeoutSeconds 240
```

Exit code: 0.

Key output:

```text
API ready: http://127.0.0.1:8000/health/live
Prometheus ready: http://127.0.0.1:9090/-/ready
Neo4j ready: http://127.0.0.1:7474/
infra-api-1          Up
infra-grafana-1      Up
infra-neo4j-1        Up
infra-postgres-1     Up
infra-prometheus-1   Up
infra-redis-1        Up
infra-redpanda-1     Up
```

The script cleaned up the compose network and containers after the smoke.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login`.
