---
id: RR-20260504-002
task: TC-20260504-002
created_at: 2026-05-04
status: approved
repo: fraud-v2
area: planning
approval: yellow
---

# Run Record: Deepen Fraud V2 Production Specs

## Assignment

Make the Fraud V2 plans more specific, identify what remains vague, compare
possible solutions and tradeoffs, map dependencies, specify data inputs, and
make the local laptop constraints explicit.

## What Changed

- Added `docs/plan-index.md`.
- Added `docs/vagueness-register.md`.
- Added `docs/solution-tradeoffs.md`.
- Added `docs/dependency-map.md`.
- Added `docs/data-strategy.md`.
- Added `docs/llm-synthetic-data-lab.md`.
- Added `docs/local-production-profile.md`.
- Updated `README.md` to link all plan docs.
- Updated `docs/spec-plan.md` with companion spec links.
- Updated `docs/setup.md` with local run profiles.
- Updated `factory/dashboard.md`.

## Research Used

Sources reviewed included:

- BryansLab Fraud Detection V2 article.
- OpenAI Structured Outputs, Batch API, Evals, and GPT-5.5 docs.
- Azure OpenAI structured outputs and batch docs.
- FastAPI, Pydantic, SQLModel, Alembic docs.
- Redpanda, Quix Streams, Apache Flink, Feast docs.
- Neo4j GDS, PyTorch Geometric, scikit-learn calibration, XGBoost docs.
- CFPB, FinCEN, NIST AI RMF sources.
- Public fraud data references: IEEE-CIS, PaySim, BankSim, Elliptic.

## Key Decisions

| Area | Decision |
|---|---|
| Product wedge | Instant-cash fintech simulation. |
| Data | Hybrid deterministic simulator, LLM scenario generation, public benchmark datasets. |
| ML | Rules plus calibrated XGBoost/LightGBM baseline; GNN optional later. |
| LLM | Generate scenarios and edge cases; do not make final fraud decisions. |
| Streams | Redpanda plus Python workers for full profile. |
| Feature store | Redis plus DuckDB/Parquet first; Feast-compatible abstractions. |
| Graph | Neo4j full profile plus NetworkX fallback. |
| Local run | Lite, full, and ML profiles. |

## Policy Boundary

Green actions:

- Read docs and public sources.
- Write local markdown plans.

Yellow actions:

- Chose more concrete defaults for local V1.
- Expanded local architecture and implementation sequence.

Red actions not taken:

- No dependency install.
- No product code.
- No commit or push.
- No remote repo.
- No vendor call.
- No real PII.
- No legal filing.

## Proof Captured

Command:

```powershell
Get-ChildItem docs
```

Exit code: 0

Output:

```text
Name                        Length
----                        ------
data-strategy.md              9094
dependency-map.md             7034
llm-synthetic-data-lab.md     7878
local-production-profile.md   5144
plan-index.md                 5980
setup.md                      8637
solution-tradeoffs.md        10034
spec-plan.md                 51506
vagueness-register.md         8998
```

Command:

```powershell
rg -n "<Code Factory banned filler phrase pattern>" README.md docs factory
```

Exit code: 1. No matches after replacing the literal scan command in this run
record.

Command:

```powershell
rg -n "<placeholder token scan>" docs README.md factory
```

Exit code: 1. No matches. Local cost placeholders were replaced with explicit
simulation defaults.

Command:

```powershell
<PowerShell regex scan for non-ASCII characters across changed markdown files>
```

Exit code: 0. No non-ASCII characters reported.

Command:

```powershell
git status --short
```

Exit code: 1. `fraud-v2` is not a git repository yet.

## Blockers Remaining

- Bryan must approve or change the instant-cash fintech wedge.
- Real production use still needs legal, privacy, security, vendor, label, and
  unit economics decisions.

## Suggested Next Task

Implement M1 contracts and synthetic generator.
