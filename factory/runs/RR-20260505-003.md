---
id: RR-20260505-003
task: TC-20260505-003
created_at: 2026-05-05
status: needs_review
repo: fraud-v2
area: implementation
approval: yellow
---

# Run Record: Outbox And Converters

## Assignment

Add production-shaped ingestion mechanics without requiring external services
for the default local path.

## What Changed

- Added `OutboxMessage` and `OutboxStatus` domain contracts.
- Added SQLite outbox persistence in the same transaction as event insert.
- Added `OutboxWorker` and `DryRunEventPublisher`.
- Added `fraud-v2 outbox-drain`.
- Added mock KYC, device-intelligence, and consortium connector boundaries.
- Added raw application/payment converters into canonical events.
- Bumped package/API version to `0.3.0`.
- Added tests for outbox idempotency, publish success, dead-letter behavior,
  converter validation, and mock connector signals.

## Proof Captured

```powershell
uv sync --extra dev --extra infra --extra llm
```

Exit code: 0.

```powershell
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
```

Exit code: 0.

Key output:

```text
66 files already formatted
All checks passed!
Success: no issues found in 57 source files
.......................                                                  [100%]
```

```powershell
uv run fraud-v2 load data/synthetic/tiny/events.jsonl --db-path data/local/outbox-smoke.sqlite
uv run fraud-v2 outbox-drain --db-path data/local/outbox-smoke.sqlite --batch-size 1000 --dry-run
```

Exit code: 0.

Key output:

```json
{
  "outbox": {
    "PUBLISHED": 756
  },
  "published": 756,
  "scanned": 756
}
```

```powershell
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
```

Exit code: 0. Final image installed `fraud-v2==0.3.0`.

```powershell
docker run -d -p 8010:8000 fraud-v2:local
Invoke-RestMethod -Uri http://127.0.0.1:8010/health/live
docker stop <container>
docker rm <container>
```

Exit code: 0.

Output:

```text
status
------
live
```

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login`.

## Remaining Gap To Full Blog

- Docker-backed integration tests for Postgres, Redis, Redpanda, and Neo4j.
- Compliance draft export workflows.
- Cost-weighted model threshold optimizer.
- Analyst UI beyond the current dashboard.
- Production RBAC, secrets, retention, and legal review.
