---
id: RR-20260505-033
task: TC-20260505-033
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add PaySim Dataset Converter

## Assignment

Add a public-dataset converter for manually downloaded PaySim-style CSV files.

## What Changed

- Added `convert_paysim_csv`.
- Added `fraud-v2 public-dataset-convert paysim`.
- Converted PaySim rows into canonical payment attempted, payment settled,
  chargeback, and label events.
- Hashed PaySim source account names into stable local IDs.
- Ignored `data/public/` so downloaded or converted datasets are not committed.
- Added unit coverage for conversion, row limits, missing columns, and source
  identifier hashing.
- Updated docs, PR draft, and version metadata to `0.33.0`.

## Proof Captured

Fresh local verification passed:

```powershell
uv run ruff format .
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
uv run pytest --collect-only -q
uv run fraud-v2 public-dataset-convert paysim data\public\raw\paysim-smoke.csv --output-path data\public\converted\paysim-smoke-events.jsonl
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Observed proof:

- Ruff format/check: passed.
- Ruff lint: passed.
- Mypy: passed across 80 source files.
- Pytest: passed with 89 collected tests.
- PaySim converter CLI smoke: 2 rows produced 5 canonical events with 1 fraud
  row, 1 legitimate row, and 0 skipped rows.
- Docker Compose full-profile config: passed.
- Docker image build: passed for `fraud-v2:local` and installed
  `fraud-v2==0.33.0`.
- Full-profile smoke: passed with API/Postgres/Redis/Neo4j/Redpanda/
  Grafana/Prometheus checks.
- Stream supervisor proof still passes: `status: completed`,
  `completed_batches: 1`, `failed_batches: 0`, and `ingested: 1`.
- Stream lag proof still passes after valid consume: total lag `0`.
- Invalid stream proof still passes: `dead_lettered: 1`,
  `dead_letter_published: 1`, `failed: 1`, and Redpanda DLQ topic proof
  printed `redpanda_dlq=1`.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
