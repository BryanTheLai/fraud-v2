---
id: RR-20260505-040
task: TC-20260505-040
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add SQLite Backup Restore

## Assignment

Add local SQLite backup and restore commands with verification.

## What Changed

- Added `write_sqlite_backup`.
- Added `restore_sqlite_backup`.
- Added `fraud-v2 sqlite-backup` and `fraud-v2 sqlite-restore`.
- Added overwrite protection for restore.
- Added unit coverage for backup/restore round trip.
- Updated docs, PR draft, and version metadata to `0.40.0`.

## Proof Captured

- `uv run ruff format .`: pass.
- `uv run ruff format --check .`: pass.
- `uv run ruff check .`: pass.
- `uv run fraud-v2 secrets-scan --root .`: pass, 238 files scanned, zero findings.
- `uv run mypy src`: pass, 86 source files checked.
- `uv run pytest -q`: pass.
- `uv run pytest --collect-only -q`: pass, 104 collected tests.
- SQLite backup/restore smoke: generated 30 users, loaded SQLite, wrote a
  verified backup manifest, restored to a new SQLite file, and returned
  `verified: true`.
- `docker compose -f infra\docker-compose.yml --profile full config --quiet`: pass.
- `docker build -t fraud-v2:local .`: pass, installed `fraud-v2==0.40.0`.
- `powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1 -TimeoutSeconds 240`:
  pass. Smoke covered API, Postgres, Redis, Neo4j, Redpanda, Grafana,
  Prometheus, audit archive proof with valid hash chain, zero stream lag,
  healthy stream report, DLQ proof, and local trace report proof.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
