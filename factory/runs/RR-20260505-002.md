---
id: RR-20260505-002
task: TC-20260505-002
created_at: 2026-05-05
status: needs_review
repo: fraud-v2
area: implementation
approval: yellow
---

# Run Record: Full Profile Adapters And Reports

## Assignment

Reduce the gap between lite-mode MVP and the full Fraud V2 architecture without
breaking laptop runability.

## What Changed

- Added `AGENTS.md` and `startup/PROMPT.md` as the repo operating contract.
- Added optional Postgres, Redis, Redpanda, and Neo4j adapter boundaries.
- Added replay and monitoring report CLIs.
- Added public dataset registry placeholders with explicit access notes.
- Added LLM synthetic scenario generation with offline default and an
  OpenAI/Azure-compatible provider boundary.
- Added JSON CLI output for automation.
- Added CI optional-extra install and Docker compose/build checks.
- Bumped package/version metadata to `0.2.0`.
- Added tests for reports, optional imports, public dataset registry, and LLM
  novelty ledger corruption tolerance.

## Proof Captured

```powershell
uv sync --extra dev --extra infra --extra llm
```

Exit code: 0.

```powershell
uv run ruff format .
uv run ruff check .
uv run mypy src
uv run pytest -q
```

Exit code: 0.

Key output:

```text
57 files left unchanged
All checks passed!
Success: no issues found in 50 source files
...............                                                          [100%]
```

```powershell
uv run fraud-v2 replay --events-path data/synthetic/tiny/events.jsonl --db-path data/local/replay.sqlite --report-path data/local/replay-report.json
```

Exit code: 0.

Key output:

```json
{
  "decisions": 120,
  "events": 756,
  "green": 82,
  "red": 14,
  "users": 120,
  "yellow": 24
}
```

```powershell
uv run fraud-v2 monitor --events-path data/synthetic/tiny/events.jsonl --db-path data/local/monitor.sqlite --output-path data/local/monitoring-report.json
```

Exit code: 0.

Key output:

```json
{
  "confusion_proxy": {
    "fn": 0,
    "fp": 0,
    "tn": 106,
    "tp": 14
  },
  "rows": 120
}
```

```powershell
uv run fraud-v2 llm-stub
uv run fraud-v2 llm-generate --provider offline
uv run fraud-v2 public-dataset paysim
```

Exit code: 0.

```powershell
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
```

Exit code: 0.

The final Docker build installed `fraud-v2==0.2.0`.

```powershell
docker run -d -p 8010:8000 fraud-v2:local
Invoke-RestMethod -Uri http://127.0.0.1:8010/health/live
docker stop <container>
docker rm <container>
```

Exit code: 0.

Output:

```text
status
------
live
```

## GitHub Status

- Feature branch prepared locally: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login`.

## Remaining Gap To Full Blog

- Real worker loops.
- Transactional outbox table and redelivery processor.
- Full-profile Docker integration tests.
- Analyst UI beyond the current FastAPI HTML dashboard.
- Real model registry, calibration report, and cost threshold optimizer.
- SAR/adverse-action draft exports that require human review.
- Production auth/RBAC and secrets handling.
