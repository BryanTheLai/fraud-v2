---
id: RR-20260505-039
task: TC-20260505-039
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Audit Archive

## Assignment

Add a local audit archive export with manifest hash and chain verification.

## What Changed

- Added `write_audit_archive`.
- Added `fraud-v2 audit-archive`.
- Added unit coverage for JSONL and manifest output.
- Expanded full smoke to prove Postgres-backed audit archive generation.
- Updated docs, PR draft, and version metadata to `0.39.0`.

## Proof Captured

- `uv run ruff format .`: pass
- `uv run ruff format --check .`: pass, 117 files already formatted
- `uv run ruff check .`: pass
- `uv run fraud-v2 secrets-scan --root .`: pass, 234 files scanned, zero findings
- `uv run mypy src`: pass, 85 source files
- `uv run pytest -q`: pass
- `uv run pytest --collect-only -q`: pass, 102 collected tests
- `uv run fraud-v2 generate --users 30 --output data\local\audit-archive-smoke-events.jsonl; uv run fraud-v2 load data\local\audit-archive-smoke-events.jsonl --db-path data\local\audit-archive-smoke.sqlite; uv run fraud-v2 audit-archive --db-path data\local\audit-archive-smoke.sqlite --output-dir data\local\audit-archive-smoke`: pass, 191 entries exported, chain valid, archive SHA-256 and root hash present
- `docker compose -f infra\docker-compose.yml --profile full config --quiet`: pass
- `docker build -t fraud-v2:local .`: pass, installed `fraud-v2==0.39.0`
- `powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1 -TimeoutSeconds 240`: pass, including Postgres audit archive proof with `entries: 196`, `chain_valid: true`, and `archive_sha256` present.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
