---
id: RR-20260505-001
task: TC-20260505-001
created_at: 2026-05-05
status: needs_review
repo: fraud-v2
area: implementation
approval: yellow
---

# Run Record: Implement Local Fraud V2 MVP

## Assignment

Build a runnable local MVP from the approved Fraud V2 specs.

## What Changed

- Initialized `fraud-v2` as a local git repo on branch `main`.
- Added Python project metadata, lockfile, `.gitignore`, `.dockerignore`, and
  `.env.example`.
- Added domain enums, entity refs, event contracts, decision contracts, review
  contracts, and domain errors.
- Added deterministic synthetic data generator and committed tiny synthetic
  event file.
- Added SQLite lite storage with idempotent event ingest, decisions, review
  cases, and review decisions.
- Added feature builder, NetworkX graph service, rules engine, decision engine,
  review service, and compliance-safe reason helper.
- Added baseline sklearn random forest training/evaluation CLI.
- Added FastAPI app with health, metrics, dashboard, event ingest, synthetic
  generation, scoring, decisions, graph neighborhood, and review endpoints.
- Added LLM synthetic data lab novelty ledger and offline stub provider.
- Added tests for domain contracts, synthetic data, decision engine, and API.
- Added Dockerfile, Docker Compose full-infra scaffold, Prometheus config,
  GitHub Actions CI, PR template, scripts, README, and changelog.

## Proof Captured

```powershell
uv sync --extra dev
```

Exit code: 0. Installed dependencies and wrote `uv.lock`.

```powershell
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
```

Exit code: 0.

Output:

```text
41 files already formatted
All checks passed!
Success: no issues found in 37 source files
.......                                                                  [100%]
```

```powershell
uv run fraud-v2 generate --users 120 --output data/synthetic/tiny/events.jsonl
```

Exit code: 0.

Output:

```text
{'events': 756, 'output': 'data\\synthetic\\tiny\\events.jsonl'}
```

```powershell
uv run fraud-v2 load data/synthetic/tiny/events.jsonl --db-path data/local/fraud_v2.sqlite
```

Exit code: 0.

Output:

```text
{'inserted': 756, 'db': 'data\\local\\fraud_v2.sqlite'}
```

```powershell
uv run fraud-v2 score user_00000 --db-path data/local/fraud_v2.sqlite
```

Exit code: 0. Returned `risk_score: 100`, `risk_tier: RED`,
`action: BLOCK`, and safe reasons.

```powershell
uv run fraud-v2 train --events-path data/synthetic/tiny/events.jsonl --output-dir data/models/baseline
```

Exit code: 0.

Key output:

```text
model_family: sklearn_random_forest
rows: 120
average_precision: 1.0
brier_score: 0.1340362756881632
precision_at_threshold: 1.0
recall_at_threshold: 1.0
f1_at_threshold: 1.0
threshold: 0.39999999999999997
```

```powershell
python TestClient smoke for /, /dashboard, /metrics, /health/ready
```

Exit code: 0.

Output:

```text
root 200
dashboard 200
metrics 200
ready {'status': 'ready', 'events': 756}
```

```powershell
docker compose -f infra/docker-compose.yml --profile full config --quiet
```

Exit code: 0.

```powershell
docker build -t fraud-v2:local .
```

Exit code: 0.

```powershell
docker run -d -p 8010:8000 fraud-v2:local
Invoke-RestMethod -Uri http://127.0.0.1:8010/health/live
docker stop <container>
docker rm <container>
```

Exit code: 0.

Output:

```json
{"status":"live"}
```

## GitHub Status

- `gh` is installed.
- `gh auth status` reported no logged-in GitHub host.
- Push and PR creation are blocked until `gh auth login` is completed.
- GitHub Actions workflow and PR template were added locally.
- Local commit created: `fa1d3f8 Implement local fraud v2 MVP`.

## Policy Boundary

Green actions:

- local files
- local tests
- local Docker build/run

Yellow actions:

- initialized local git repo
- added CI workflow
- created local commit path

Red actions not completed:

- no push
- no PR
- no real vendor calls
- no real PII
- no money movement
- no compliance filing

## Remaining Gap To Full Blog

The local MVP runs. The full Fraud V2 blog still needs later milestones:

- Redpanda workers
- Redis online feature store
- Neo4j projection
- Postgres production store
- analyst workflow polish
- real shadow model registry
- LLM batch generation provider
- public dataset loaders
- fairness and drift reports
- production auth/RBAC

## Suggested Next Task

Implement M2 full-profile infrastructure adapters:

- Postgres repository
- Redis feature cache
- Redpanda outbox publisher
- worker processes
- Neo4j graph projection
- integration tests using Docker services
