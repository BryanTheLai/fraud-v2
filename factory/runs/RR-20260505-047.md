---
id: RR-20260505-047
task: TC-20260505-047
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: operations
approval: green
---

# Run Record: Clean And Tidy

## Assignment

Reduce repo operating noise while preserving source, specs, tests, and proof.

## What Changed

- Added `scripts\verify.ps1` as one local verification entry point.
- Added `scripts\clean-local.ps1` for repo-scoped cleanup of ignored caches and
  generated smoke artifacts.
- Made `scripts\test.ps1` delegate to `verify.ps1`.
- Moved completed task cards from `factory/tasks/active` to
  `factory/tasks/review`.
- Compacted the Code Factory dashboard.
- Updated docs and PR templates to point at the new scripts.
- Updated version metadata to `0.47.0`.
- Removed stale ignored `.venv` metadata for `fraud_v2-0.36.0` after
  verifying `uv run` resolves the package at `0.47.0`.

## Proof Captured

- `powershell -ExecutionPolicy Bypass -File scripts\verify.ps1`: pass.
- `powershell -ExecutionPolicy Bypass -File scripts\verify.ps1 -Full`: pass.
- `uv run ruff format --check .`: pass, 131 files already formatted.
- `uv run ruff check .`: pass.
- `uv run fraud-v2 secrets-scan --root .`: pass, 268 scanned files,
  zero findings.
- `uv run mypy src`: pass, 91 source files.
- `uv run pytest -q`: pass, 123 tests.
- `uv run pytest --collect-only -q`: pass, 123 collected tests.
- `uv run fraud-v2 local-doctor --output-path data\local\local-doctor-smoke.json --dashboard-path data\local\local-doctor-smoke.html`:
  pass, wrote JSON/HTML, reported `lite_ready: true`,
  `full_profile_ready: true`, `github_handoff_ready: false`, RTX 3050 Laptop
  GPU visible, 13.9 GiB RAM detected, and 41.3 GiB free disk.
- `uv run fraud-v2 readiness-report --output-path data\local\readiness-report-smoke.json --dashboard-path data\local\readiness-report-smoke.html`:
  pass, wrote JSON/HTML, reported 8 checks, 20 implemented capabilities,
  7 production blockers, and blocked status from missing GitHub remote/auth.
- `uv run fraud-v2 release-runbook --output-path data\local\release-runbook-smoke.md`:
  pass, wrote a 2,356-byte Markdown runbook.
- `uv run fraud-v2 capacity-profile --profile smoke --users 50 --score-users 5 --min-load-events-per-second 0.1 --min-score-decisions-per-second 0.1 --output-dir data\local\ci-capacity --overwrite --fail-on-target-miss`:
  pass, 50 users, 316 events, 86.404 load events/sec,
  37.495 score decisions/sec.
- `docker compose -f infra\docker-compose.yml --profile full config --quiet`:
  pass.
- `docker build -t fraud-v2:local .`: pass, installed
  `fraud-v2==0.47.0`.
- `powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1 -TimeoutSeconds 240`:
  pass, including Postgres backup/restore 194/194 verified, audit archive
  chain valid over 196 entries, Redpanda valid consume, DLQ proof for invalid
  record, stream health `healthy` with score 100, and 18 local request traces.
- `powershell -ExecutionPolicy Bypass -File scripts\clean-local.ps1 -DryRun`:
  pass and listed only ignored caches, generated smoke artifacts, and project
  `__pycache__` directories.
- `powershell -ExecutionPolicy Bypass -File scripts\clean-local.ps1`: pass,
  removed ignored artifacts and kept `.venv` and `data\public`. The first pass
  skipped a locked local SQLite file; after stopping the local uvicorn process,
  the final pass removed `data\local`.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
