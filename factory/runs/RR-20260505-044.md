---
id: RR-20260505-044
task: TC-20260505-044
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Release Runbook

## Assignment

Add a generated local release runbook for operator handoff.

## What Changed

- Added `fraud_v2.operations.release_runbook`.
- Added `fraud-v2 release-runbook`.
- Added Markdown output with version, branch, latest commit, lite/full-mode
  commands, verification gates, recovery rehearsals, GitHub handoff, and hard
  limits.
- Added unit coverage for rendering and file writing.
- Updated docs, PR draft, and version metadata to `0.44.0`.

## Proof Captured

- `uv run ruff format .`: pass.
- `uv run ruff format --check .`: pass, 126 files already formatted.
- `uv run ruff check .`: pass.
- `uv run fraud-v2 secrets-scan --root .`: pass, 255 files scanned, zero findings.
- `uv run mypy src`: pass, 89 source files checked.
- `uv run pytest -q`: pass.
- `uv run pytest --collect-only -q`: pass, 113 collected tests.
- `uv run fraud-v2 release-runbook --output-path data\local\release-runbook-smoke.md`:
  pass, wrote a 2,356-byte Markdown runbook.
- `uv run fraud-v2 capacity-profile --profile smoke --users 50 --score-users 5 --min-load-events-per-second 0.1 --min-score-decisions-per-second 0.1 --output-dir data\local\ci-capacity --overwrite --fail-on-target-miss`:
  pass. It generated 316 events, loaded at 117.292 events/sec, scored at 56.446
  decisions/sec, and wrote JSON/HTML artifacts.
- `docker compose -f infra\docker-compose.yml --profile full config --quiet`: pass.
- `docker build -t fraud-v2:local .`: pass, installed `fraud-v2==0.44.0`.
- `powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1 -TimeoutSeconds 240`:
  pass. Smoke covered API, Postgres, Redis, Neo4j, Redpanda, Grafana,
  Prometheus, audit archive proof with 196 valid hash-chain entries, Postgres
  backup rehearsal with an 80,152-byte `pg_dump_custom` artifact, source/restored
  event counts of 194/194, `verified: true`, zero stream lag, healthy stream
  report with `health_score: 100`, DLQ proof, and local trace report proof with
  17 spans.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
