---
id: RR-20260505-031
task: TC-20260505-031
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Local Load Benchmark

## Assignment

Add a local synthetic load benchmark receipt for laptop validation.

## What Changed

- Added `run_load_benchmark`.
- Added `fraud-v2 load-benchmark`.
- Added generation, load, and scoring timing metrics.
- Added throughput metrics for inserted events and scored decisions.
- Added risk-tier counts and runtime platform metadata.
- Added Windows-safe overwrite behavior by clearing benchmark tables instead
  of deleting a potentially locked SQLite file.
- Added unit coverage for report writing, overwrite refusal, and overwrite
  replacement.
- Updated docs, PR draft, and version metadata to `0.31.0`.

## Proof Captured

Fresh local verification passed:

```powershell
uv run ruff format .
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
uv run pytest --collect-only -q
uv run fraud-v2 load-benchmark --users 10 --score-users 2 --db-path data\local\load-benchmark-smoke.sqlite --output-path data\local\load-benchmark-smoke.json --overwrite
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Observed proof:

- Ruff format/check: passed.
- Ruff lint: passed.
- Mypy: passed across 78 source files.
- Pytest: passed with 83 collected tests.
- Load benchmark CLI smoke: `10` users generated `62` events, inserted `62`
  events, scored `2` users, and wrote `data\local\load-benchmark-smoke.json`.
- Docker Compose full-profile config: passed.
- Docker image build: passed for `fraud-v2:local` and installed
  `fraud-v2==0.31.0`.
- Full-profile smoke: passed with API/Postgres/Redis/Neo4j/Redpanda/
  Grafana/Prometheus checks.
- Stream supervisor proof still passes: `status: completed`,
  `completed_batches: 1`, `failed_batches: 0`, and `ingested: 1`.
- Stream lag proof still passes after valid consume: total lag `0`.
- Invalid stream proof still passes: `dead_lettered: 1`,
  `dead_letter_published: 1`, `failed: 1`, and Redpanda DLQ topic proof
  printed `redpanda_dlq=1`.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
