---
id: RR-20260505-026
task: TC-20260505-026
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Policy Promotion Registry

## Assignment

Add local threshold policy registration and promotion controls.

## What Changed

- Added `JsonThresholdPolicyRegistry`.
- Added candidate, active, and disabled policy statuses.
- Added policy SHA-256 hashing at registration time.
- Added one-active-policy promotion behavior.
- Added active policy JSON export for `FRAUD_POLICY_PATH`.
- Added `policy-register`, `policy-list`, and `policy-promote` CLI commands.
- Added unit coverage for registration, promotion, active export, and missing
  policy failure.
- Updated docs, PR draft, and version metadata to `0.26.0`.

## Proof Captured

Fresh local verification passed:

```powershell
uv run ruff format .
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Observed proof:

- Ruff format/check: passed.
- Ruff lint: passed.
- Mypy: passed across 73 source files.
- Pytest: passed with 68 tests.
- Docker Compose full-profile config: passed.
- Docker image build: passed for `fraud-v2:local` and installed
  `fraud-v2==0.26.0`.
- Full-profile smoke: passed with API/Postgres/Redis/Neo4j/Redpanda/
  Grafana/Prometheus checks.
- Stream proof remained green: valid consume reported `dead_lettered: 0`,
  `ingested: 1`, and `failed: 0`.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
