---
id: RR-20260505-043
task: TC-20260505-043
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add GitHub Handoff

## Assignment

Add an executable GitHub push/PR handoff path that remains safe while auth and
remote are missing.

## What Changed

- Added `scripts/github-handoff.ps1`.
- Added JSON blocker reporting for remote, GitHub auth, PR draft, and worktree
  readiness.
- Added exact next commands for `gh auth login`, `git remote add`, `git push`,
  and `gh pr create`.
- Added `-Execute` mode that refuses to push unless all gates are green.
- Added unit coverage for execute guards and blocker reporting.
- Updated docs, PR draft, and version metadata to `0.43.0`.

## Proof Captured

- `uv run ruff format .`: pass.
- `uv run ruff format --check .`: pass, 123 files already formatted.
- `uv run ruff check .`: pass.
- `uv run fraud-v2 secrets-scan --root .`: pass, 250 files scanned, zero findings.
- `uv run mypy src`: pass, 87 source files checked.
- `uv run pytest -q`: pass.
- `uv run pytest --collect-only -q`: pass, 111 collected tests.
- PowerShell parser check for `scripts\github-handoff.ps1`,
  `scripts\postgres-backup-rehearsal.ps1`, and `scripts\full-smoke.ps1`: pass.
- `powershell -ExecutionPolicy Bypass -File scripts\github-handoff.ps1`: pass.
  It reported `remote_configured: false`, `gh_authenticated: false`,
  `body_file_exists: true`, `worktree_clean: true` after the local commit,
  `ready: false`, and exact next commands.
- `uv run fraud-v2 capacity-profile --profile smoke --users 50 --score-users 5 --min-load-events-per-second 0.1 --min-score-decisions-per-second 0.1 --output-dir data\local\ci-capacity --overwrite --fail-on-target-miss`:
  pass. It generated 316 events, loaded at 125.557 events/sec, scored at 49.029
  decisions/sec, and wrote JSON/HTML artifacts.
- `docker compose -f infra\docker-compose.yml --profile full config --quiet`: pass.
- `docker build -t fraud-v2:local .`: pass, installed `fraud-v2==0.43.0`.
- `powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1 -TimeoutSeconds 240`:
  pass. Smoke covered API, Postgres, Redis, Neo4j, Redpanda, Grafana,
  Prometheus, audit archive proof with 196 valid hash-chain entries, Postgres
  backup rehearsal with an 80,070-byte `pg_dump_custom` artifact, source/restored
  event counts of 194/194, `verified: true`, zero stream lag, healthy stream
  report with `health_score: 100`, DLQ proof, and local trace report proof with
  17 spans.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
