---
id: RR-20260505-021
task: TC-20260505-021
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Retention Pruning

## Assignment

Add explicit local retention pruning for expired records while preserving safe
defaults and audit hash-chain validity.

## What Changed

- Added `FraudStore.prune_retention`.
- Implemented explicit retention pruning for SQLite and Postgres.
- Kept `retention_report` report-only.
- Added admin-only `POST /v1/retention/prune`.
- Added `execute=false` dry-run default for the API.
- Added `fraud-v2 retention-prune` with dry-run default and explicit
  `--execute`.
- Preserved audit entries during pruning so audit hash-chain verification stays
  valid.
- Expanded unit tests and full-profile smoke.
- Bumped package/API version to `0.21.0`.

## Proof Captured

Fresh local verification passed:

```powershell
uv run ruff format .
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Observed proof:

- Ruff format/check: passed.
- Ruff lint: passed.
- Mypy: passed across 68 source files.
- Pytest: passed with 54 tests.
- Docker Compose full-profile config: passed.
- Docker image build: passed for `fraud-v2:local` and installed
  `fraud-v2==0.21.0`.
- Full-profile smoke: passed.
- Full-profile smoke proved retention prune dry-run, execute, audit-chain
  preservation, and the existing API/Postgres/Redis/Neo4j/Redpanda/Grafana/
  Prometheus checks.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
