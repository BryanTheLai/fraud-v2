---
id: RR-20260505-014
task: TC-20260505-014
created_at: 2026-05-05
status: needs_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Retention Reporting

## Assignment

Add dry-run local retention reporting so operators can see what data would be
eligible for expiry by table.

## What Changed

- Added `RetentionPolicy`, `RetentionTableReport`, and `RetentionReport` domain
  models.
- Added `SQLiteStore.retention_report()`.
- Added admin-only `GET /v1/retention/report`.
- Added `fraud-v2 retention-report`.
- Expanded full smoke to verify retention report loading.
- Added unit and integration tests.
- Updated setup, README, production-readiness, and PR draft docs.
- Bumped package/API version to `0.14.0`.

## Proof Captured

```powershell
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
```

Exit code: 0.

Key output:

```text
82 files already formatted
All checks passed!
Success: no issues found in 66 source files
............................................                             [100%]
```

```powershell
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
```

Exit code: 0. Docker build installed `fraud-v2==0.14.0`.

```powershell
.\scripts\full-smoke.ps1 -TimeoutSeconds 240
```

Exit code: 0.

Key output:

```text
API ready: http://127.0.0.1:18000/health/live
Prometheus ready: http://127.0.0.1:19090/-/ready
Grafana ready: http://127.0.0.1:13000/api/health
Neo4j ready: http://127.0.0.1:17474/
Prometheus query ready: up{job="fraud-v2-api", instance="api:8000"}
```

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
