---
id: RR-20260504-001
task: TC-20260504-001
created_at: 2026-05-04
status: approved
repo: fraud-v2
area: repo-bootstrap
approval: yellow
---

# Run Record: Build Fraud V2 Spec And Setup

## Assignment

Create a full local build spec for Fraud V2 using Bryan's startup prompt and
Code Factory protocol.

## What Happened

- Read `startup/PROMPT.md`.
- Read Code Factory `AGENTS.md`, `docs/principles.md`, `docs/protocol.md`,
  `templates/repo-spec-plan.md`, `templates/repo-setup.md`,
  `docs/style.md`, and `docs/folder-layout.md`.
- Fetched the BryansLab Fraud V2 article content.
- Inspected the local `fraud-v2` folder.
- Checked local CPU, GPU, Python, uv, Docker, and NVIDIA driver/CUDA state.
- Created the spec, setup doc, README, dashboard, Task Card, and Run Record.

## Files Changed

- `README.md`
- `docs/spec-plan.md`
- `docs/setup.md`
- `factory/dashboard.md`
- `factory/tasks/done/TC-20260504-001-build-fraud-v2-spec-and-setup.md`
- `factory/runs/RR-20260504-001.md`

## Proof Captured

Commands run:

```powershell
Get-ChildItem -Force
rg --files -g "PROMPT.md" -g "*code-factory*" -g "*CodeFactory*"
Get-Content -Raw startup\PROMPT.md
Get-Content -Raw code-factory\docs\principles.md
Get-Content -Raw code-factory\docs\protocol.md
Get-Content -Raw code-factory\templates\repo-spec-plan.md
Get-Content -Raw code-factory\templates\repo-setup.md
Get-CimInstance Win32_Processor
Get-CimInstance Win32_VideoController
nvidia-smi
python --version
uv --version
docker --version
```

Observed local environment:

```text
CPU: AMD Ryzen 7 5800H with Radeon Graphics, 8 cores, 16 logical processors
GPU: NVIDIA GeForce RTX 3050 Laptop GPU, 4096 MiB VRAM
Driver: 565.90
CUDA: 12.7
Python: 3.12.9
uv: 0.7.3
Docker: 28.4.0
```

Final artifact proof:

```powershell
Get-ChildItem -Recurse docs
```

Exit code: 0

Output:

```text
Directory: C:\Users\wbrya\OneDrive\Documents\GitHub\fraud-v2\docs

Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
-a----         05-May-26  12:36 AM           8197 setup.md
-a----         05-May-26  12:36 AM          50107 spec-plan.md
```

Writing scan:

```powershell
rg -n "<Code Factory banned filler phrase pattern>" README.md docs factory
```

Exit code: 1. No matches.

ASCII scan:

```powershell
<PowerShell regex scan for non-ASCII characters across changed markdown files>
```

Exit code: 0. No non-ASCII characters reported.

Git status:

```powershell
git status --short
```

Exit code: 1. `fraud-v2` is not a git repository yet.

## Assumptions Checked

| Assumption | Result |
|---|---|
| `fraud-v2` was safe for docs/bootstrap artifacts. | Folder existed and had no visible files before writing. |
| Local machine can run a Python-first local stack. | Python, uv, Docker, and NVIDIA driver are present. |
| GPU should not be required for serving. | RTX 3050 has 4GB VRAM, so CPU fallback is required. |

## Policy Boundary

Green actions taken:

- Read local files.
- Research public docs.
- Create local markdown artifacts.
- Create local empty project folders.

Yellow actions taken:

- Created Code Factory folders inside `fraud-v2`.
- Chose a recommended technical stack.

Red actions not taken:

- No dependency install.
- No commit.
- No push.
- No remote repo creation.
- No vendor calls.
- No real PII.
- No money movement.
- No legal/compliance filing.

## Blockers

- Bryan has not chosen the first fraud wedge.
- Real production domain, regulatory scope, data vendors, labels, and action
  authority remain open.

## Lesson Recommendation

No Lesson yet. This run created a spec, not repeated operational evidence.

## Suggested Next Task Card

Create M1 domain contracts and synthetic event generator:

- define enums and Pydantic classes
- create synthetic users/devices/sessions/transactions/applications
- implement raw-to-canonical converters for synthetic inputs
- add unit tests for valid, malformed, duplicate, and missing inputs
