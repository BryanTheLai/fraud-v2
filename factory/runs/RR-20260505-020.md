---
id: RR-20260505-020
task: TC-20260505-020
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Use Postgres In Full Profile

## Assignment

Move Docker full mode from SQLite-backed API state to Postgres-backed API state
while preserving SQLite as the default lite path.

## What Changed

- Added `FraudStore` protocol for shared storage boundaries.
- Added `PostgresStore` for events, decisions, review cases, review decisions,
  outbox messages, audit entries, and retention reports.
- Kept `PostgresEventStore` as a compatibility alias for adapter smoke checks.
- Added `FRAUD_STORE_BACKEND` and `FRAUD_POSTGRES_DSN` settings.
- Switched Docker Compose API env to `FRAUD_STORE_BACKEND=postgres`.
- Updated decision, review, and API dependencies to accept the shared store
  protocol.
- Updated docs and PR draft.
- Bumped package/API version to `0.20.0`.

## Proof Captured

Fresh local verification passed:

```powershell
uv run ruff format .
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Observed proof:

- Ruff format/check: passed.
- Ruff lint: passed.
- Mypy: passed across 68 source files.
- Pytest: passed with 53 tests.
- Docker Compose full-profile config: passed.
- Docker image build: passed for `fraud-v2:local`.
- Full-profile smoke: passed.
- Full-profile smoke proved:
  - API health.
  - Prometheus readiness and API scrape.
  - Grafana health and provisioned dashboard load.
  - Neo4j HTTP and Bolt readiness.
  - Postgres readiness.
  - Redis readiness.
  - Redpanda readiness.
  - protected API auth.
  - synthetic event generation.
  - high-risk scoring for `user_00000`.
  - synthetic review-case seeding, review-decision submission, and closed-case
    readback.
  - audit entries and valid audit hash chain.
  - retention report loading.
  - Postgres insert/list adapter proof: `postgres_events=194`.
  - Redis feature-cache proof: `redis_feature_cache=user_00000`.
  - Neo4j graph projection proof: `neo4j_edges=50`.
  - Redpanda publish proof: `redpanda_publish=1`.
  - analyst dashboard and graph evidence dashboard rendering.
  - API metrics including decision and event counters.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
