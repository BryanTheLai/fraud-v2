---
id: RR-20260505-035
task: TC-20260505-035
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Stream Health Report

## Assignment

Add a local stream health report and dashboard artifact.

## What Changed

- Added `StreamHealthReport`, statuses, severities, thresholds, and alert
  models.
- Added `fraud-v2 stream-health`.
- Added optional JSON output paths to `stream-lag` and `stream-supervise`.
- Added static HTML stream health dashboard rendering.
- Added unit coverage for healthy, degraded, critical, and artifact-writing
  paths.
- Updated docs, PR draft, and version metadata to `0.35.0`.

## Proof Captured

- `uv run ruff format .`: pass
- `uv run ruff format --check .`: pass, 110 files already formatted
- `uv run ruff check .`: pass
- `uv run mypy src`: pass, 82 source files
- `uv run pytest -q`: pass
- `uv run pytest --collect-only -q`: pass, 95 collected tests
- `uv run fraud-v2 stream-health --db-path data\local\stream-health-smoke.sqlite --output-path data\local\stream-health-smoke.json --dashboard-path data\local\stream-health-smoke.html`: pass, wrote JSON and HTML with `status: degraded` because live lag was intentionally not checked.
- `docker compose -f infra\docker-compose.yml --profile full config --quiet`: pass
- `docker build -t fraud-v2:local .`: pass, installed `fraud-v2==0.35.0`
- `powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1 -TimeoutSeconds 240`: pass, including stream health full-profile proof with `status: healthy`, `health_score: 100`, `total_lag: 0`, `completed_batches: 1`, and `dead_letter_publish_failed: 0`.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
