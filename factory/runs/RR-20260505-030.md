---
id: RR-20260505-030
task: TC-20260505-030
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Signed Policy Approvals

## Assignment

Add local signed maker-checker style approvals for threshold-policy promotion.

## What Changed

- Added `PolicyApproval`, `PolicyApprovalStatus`, and `JsonPolicyApprovalStore`.
- Added local Ed25519 key generation for policy approval signing.
- Added signed approval creation and verification over policy version, policy
  SHA-256, approver, role, timestamp, notes, and signature algorithm.
- Added approval status checks that count distinct verified approvers.
- Added `policy-keygen`, `policy-approve`, `policy-approval-status`, and
  `policy-promote-approved` CLIs.
- Ignored generated local policy PEM keys so private approval keys are not
  accidentally staged.
- Added unit coverage for distinct approver counting, tamper detection, and
  overwrite protection.
- Updated docs, PR draft, and version metadata to `0.30.0`.

## Proof Captured

Fresh local verification passed:

```powershell
uv run ruff format .
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest -q
uv run pytest --collect-only -q
docker compose -f infra\docker-compose.yml --profile full config --quiet
docker build -t fraud-v2:local .
powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1
```

Observed proof:

- Ruff format/check: passed.
- Ruff lint: passed.
- Mypy: passed across 77 source files.
- Pytest: passed with 80 collected tests.
- Docker Compose full-profile config: passed.
- Docker image build: passed for `fraud-v2:local` and installed
  `fraud-v2==0.30.0`.
- Full-profile smoke: passed with API/Postgres/Redis/Neo4j/Redpanda/
  Grafana/Prometheus checks.
- Stream supervisor proof still passes: `status: completed`,
  `completed_batches: 1`, `failed_batches: 0`, and `ingested: 1`.
- Stream lag proof still passes after valid consume: total lag `0`.
- Invalid stream proof still passes: `dead_lettered: 1`,
  `dead_letter_published: 1`, `failed: 1`, and Redpanda DLQ topic proof
  printed `redpanda_dlq=1`.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
