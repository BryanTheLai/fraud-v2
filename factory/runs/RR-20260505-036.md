---
id: RR-20260505-036
task: TC-20260505-036
created_at: 2026-05-05
completed_at: 2026-05-05
status: waiting_for_review
repo: fraud-v2
area: implementation
approval: green
---

# Run Record: Add Local Stream Service Runner

## Assignment

Add a local Windows stream service runner that produces stream supervisor and
health artifacts.

## What Changed

- Added `scripts/local-stream-service.ps1`.
- Added dry-run, one-shot, lag-check, DLQ publish, and critical-health options.
- Added tests that pin the runner's expected CLI wiring.
- Documented local Task Scheduler setup and removal commands.
- Updated docs, PR draft, and version metadata to `0.36.0`.

## Proof Captured

- `powershell -ExecutionPolicy Bypass -File scripts\local-stream-service.ps1 -Once -DryRun`: pass, printed the `stream-supervise` and `stream-health` commands without touching Redpanda.
- `uv run ruff format .`: pass
- `uv run ruff format --check .`: pass, 111 files already formatted
- `uv run ruff check .`: pass
- `uv run mypy src`: pass, 82 source files
- `uv run pytest -q`: pass
- `uv run pytest --collect-only -q`: pass, 97 collected tests
- `docker compose -f infra\docker-compose.yml --profile full config --quiet`: pass
- `docker build -t fraud-v2:local .`: pass, installed `fraud-v2==0.36.0`
- `powershell -ExecutionPolicy Bypass -File scripts\full-smoke.ps1 -TimeoutSeconds 240`: pass, including API, Postgres, Redis, Neo4j, Redpanda, stream lag `0`, stream supervisor `completed_batches: 1`, stream health `status: healthy` and `health_score: 100`, DLQ publish proof, Grafana, Prometheus, retention prune, and audit verification.

## GitHub Status

- Feature branch: `feature/full-profile-adapters`.
- Push and PR creation still require `gh auth login` and a configured remote.
